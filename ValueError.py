while True :
    f = input("please enter your height in meters (ex: 1.5)")
    try:
        float(f)
    except ValueError:
        continue
    f = float(f)
    if f <= 1 :
        print("isn't that little bit short?")
        continue
     else:
             break
         while True :
    nn=input("please enter your age (ex: 45)")
    try:
        int(nn)
    except ValueError:
            continue
        nn= int(nn)
        if nn < 0
        print("so you are not born yet?" )
        continue
     else:
             break
         
         while True :
        st=input("please enter which fruit you would like to buy (ex: apple)")
    try:
         str(st)
         except ValueError :
             continue
         if
     
         nn=input("please enter the amount of your dept (ex: -20 pounds")
     try:
         int(nn)
         except ValueError :
             continue
             try:
         st()
         except ValueError :
             continue
         pn=input("please enter ")
         else:
             break
